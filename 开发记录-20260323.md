# ClawPanel 改造记录 (2026-03-23)

## v0.9.8 更新内容

### 新功能

#### Gateway 启动诊断 + 一键修复
- 新增 `diagnose_gateway` 接口，自动检测 6 项启动条件：CLI 二进制、配置文件、plist/systemd 状态、入口文件有效性、端口冲突、僵尸进程、最近错误日志
- 新增 `fix_gateway` 接口，自动杀残留进程、清理服务状态、重新启动并验证结果
- `start_service` 不再盲目返回成功，改为等待 2 秒后验证实际启动状态
- 仪表盘 Gateway 卡片新增"诊断"按钮，启动失败自动弹出诊断面板
- 诊断面板显示各项检查结果（✓/✗），可修复项提供"一键修复"按钮
- 全部跨平台兼容：macOS (launchctl) / Linux (systemd) / Windows (PowerShell)

#### 插件安装容错机制
- 新增 `fallbackInstallPlugin()` 备用安装方案
- 当 `openclaw plugins install` 因缺少 `openclaw.extensions` 字段失败时，自动回退到直接 `npm install` + 补丁 package.json
- 覆盖所有插件安装入口：渠道插件、QQ Bot 插件、SSE 流式安装、技能市场插件
- 安装后增加目录验证，失败时明确提示用户

#### 消息渠道扩展至 18 个平台
新增支持：微信（腾讯官方插件）、企业微信、WhatsApp、Slack、Signal、Google Chat、iMessage (BlueBubbles)、LINE、Microsoft Teams、Matrix、Mattermost、IRC、Twitch、WebChat

### 优化

#### UI 全局优化
- 新增设计 token：`--shadow-card`、`--shadow-card-hover`、`--shadow-glow`、`--bg-card-gradient`
- 按钮：focus-visible 轮廓、hover 浮起发光、active 按压反馈
- 卡片：统一阴影体系 + hover 浮起效果
- Toast 通知：彩色左边框、滑入/滑出动画、backdrop-blur
- Modal 弹窗：更强 backdrop blur (8px)
- 侧边栏：backdrop-filter blur(12px)、活跃项左侧 indigo 指示条

### 修复
- 修复 Gateway plist 入口文件路径不匹配导致无法启动的问题
- 修复 macOS `macStartService` 吞掉所有错误始终返回 `true` 的问题

### 文档
- 仓库引用全部更新为 `cangerx/clawpanel`
- 移除推广内容（赞助商广告、社区群二维码、晴辰云服务推广、公司版权声明）
- 消息渠道文档从 5 个平台更新为 18 个平台完整列表
- package.json / Cargo.toml 仓库地址同步更新

### 涉及文件

| 文件 | 改动 |
|------|------|
| `scripts/dev-api.js` | 诊断/修复 handler、start_service 改进、插件 fallback、跨平台兼容 |
| `src/pages/dashboard.js` | 诊断 UI、启动失败自动诊断 |
| `src/pages/channels.js` | 18 平台渠道注册表 |
| `src/lib/tauri-api.js` | 新增 diagnoseGateway / fixGateway API |
| `src/style/pages.css` | 诊断面板样式 |
| `src/style/variables.css` | 设计 token |
| `src/style/components.css` | 组件样式优化 |
| `src/style/layout.css` | 布局样式优化 |
| `README.md` | 仓库引用、推广清理、渠道列表 |
| `README.en.md` | 同上英文版 |
| `CONTRIBUTING.md` | 仓库引用更新 |
| `SECURITY.md` | 仓库引用更新 |
| `docs/*.md` | 部署文档仓库引用更新 |
| `package.json` | 版本号 + 仓库地址 |
| `src-tauri/tauri.conf.json` | 版本号 |
| `src-tauri/Cargo.toml` | 版本号 + 仓库地址 |

---

## 接入 mcp-wechat-server 微信 MCP 机器人

> 提交: `e8906fe` | 时间: 2026-03-23

### 背景

原微信渠道使用 `@tencent-weixin/openclaw-weixin-cli` 官方插件，改为接入开源项目 [mcp-wechat-server](https://github.com/Howardzhangdqs/mcp-wechat-server)，基于 MCP（Model Context Protocol）协议，支持扫码登录、消息轮询、文本发送、打字状态。

### 改动内容

#### 1. 微信渠道配置更新 (`src/pages/channels.js`)
- `pluginRequired` 从 `@tencent-weixin/openclaw-weixin-cli@latest` 改为 `mcp-wechat-server`
- `pluginId` 从 `wechat-clawbot` 改为 `mcp-wechat-server`
- 新增 `mcpServer: true` 标记，区分 MCP 类型插件和传统 OpenClaw 插件
- 更新安装引导文案和来源链接
- 安装流程新增 MCP 分支：Tauri 模式走 `installMcpPlugin`，Web 模式走 `installMcpPluginStream`（SSE 流式）
- 安装成功后自动读取 `mcp.json`，写入 MCP 服务配置

#### 2. 后端新增 MCP 插件安装 (`scripts/dev-api.js`)
- 新增 `install_mcp_plugin` handler：
  - 全局 `npm install -g mcp-wechat-server`
  - 自动写入 `~/.openclaw/mcp.json` 的 `mcpServers.wechat` 配置项
  - 配置内容：`{ command: "npx", args: ["mcp-wechat-server"] }`
- 新增 `mcp_plugin_install_stream` SSE handler：
  - 流式输出 npm 安装日志
  - 安装完成后自动写入 mcp.json
  - 支持客户端断开时 kill 子进程
- `get_channel_plugin_status` 增强：
  - 新增全局 npm 包检测（`npm list -g <pkg>`）
  - 新增 mcp.json 配置检测（检查 mcpServers 中是否已配置）
  - MCP 类型插件不再依赖 `~/.openclaw/plugins/` 目录

#### 3. 前端 API 层 (`src/lib/tauri-api.js`)
- 新增 `installMcpPlugin(packageName, serverId)` — 同步安装
- 新增 `installMcpPluginStream(packageName, serverId, callbacks)` — SSE 流式安装

### mcp-wechat-server 提供的 6 个 MCP 工具

| 工具 | 功能 |
|------|------|
| `login_qrcode` | 生成微信登录二维码 |
| `check_qrcode_status` | 检查二维码扫码状态 |
| `logout` | 退出登录并清除凭证 |
| `get_messages` | 拉取新消息（支持长轮询阻塞等待） |
| `send_text_message` | 发送文本消息 |
| `send_typing` | 发送/取消"正在输入"状态 |

### 用户使用流程

1. 消息渠道页面 → 点击微信 → 安装
2. ClawPanel 自动全局安装 `mcp-wechat-server` 并写入 MCP 配置
3. Gateway 重载后加载 MCP 服务
4. 通过 AI 助手调用 `login_qrcode` 生成二维码
5. 手机微信扫码绑定
6. AI 自动轮询消息、显示打字状态、回复消息

### 涉及文件

| 文件 | 改动 |
|------|------|
| `src/pages/channels.js` | 微信渠道改用 mcp-wechat-server，新增 MCP 安装分支 |
| `scripts/dev-api.js` | 新增 install_mcp_plugin、mcp_plugin_install_stream、增强 plugin status 检测 |
| `src/lib/tauri-api.js` | 新增 installMcpPlugin / installMcpPluginStream API |

---

## 微信自动回复桥接（MCP 工具 -> 自动消息渠道）

> 时间: 2026-03-23 中午

### 问题定位

用户在微信里给 Bot 发消息后没有自动回复。排查结果：

- `mcp-wechat-server` 登录成功，`check_qrcode_status` 返回 `confirmed`
- `wechat.get_messages` 能正常拉到新消息，说明 **微信 -> MCP** 收消息链路没问题
- 但 `gateway.log` 中没有任何 `wechat / mcp-wechat / send_text_message / get_messages` 相关自动处理日志
- `openclaw.json` 中也没有 `channels.wechat` 的原生消息渠道实现

结论：

- 当前微信只是作为 **MCP 工具集** 接入
- 能手动 `login_qrcode / get_messages / send_text_message`
- **不会自动把微信消息转成 OpenClaw channel event**
- 所以不会像飞书 / Telegram 那样自动路由给 Agent 再自动回消息

### 本次实现

#### 1. 在 `scripts/dev-api.js` 新增微信桥接守护循环

- 启动 `dev-api` 时自动调用 `startWechatBridgeLoop()`
- 后台每 5 秒执行一次桥接轮询
- 通过 `mcporter call wechat.get_messages --config ~/.openclaw/workspace/config/mcporter.json` 拉取新微信消息
- 按 `message_id` 去重，避免重复回复
- 已处理消息 ID 持久化到：
  - `~/.openclaw/clawpanel/wechat-bridge-state.json`

#### 2. 复用 Gateway WebSocket 协议，把微信消息送给 Agent

- 新增 `gatewayChatForWechat()`：
  - 直连本地 Gateway `ws://127.0.0.1:<port>/ws`
  - 走与实时聊天相同的 `connect.challenge -> connect -> chat.send` 协议
  - 自动为每个微信用户生成独立 session：
    - `agent:<agentId>:wechat:<sha1(userId)>`
- Agent 绑定规则复用 `openclaw.json.bindings`
  - 如未显式绑定，默认走 `main`

#### 3. 自动把 Agent 回复发回微信

- Agent 生成最终文本后，自动调用：
  - `wechat.send_typing`
  - `wechat.send_text_message`
  - `wechat.send_typing status=cancel`
- 这条链路让微信从“手动 MCP 工具”升级为“自动收发桥接”

#### 4. 新增桥接运行态诊断

- `get_channel_runtime_status('wechat')` 现在额外返回：
  - `bridge.started`
  - `bridge.busy`
  - `bridge.lastRunAt`
  - `bridge.lastMessageAt`
  - `bridge.lastError`
  - `bridge.handledCount`
- 新增后端调试接口：
  - `get_wechat_bridge_status`
  - `wechat_bridge_run_once`
  - `wechat_bridge_test_message`

#### 5. 消息渠道页面展示桥接状态

- 微信卡片现在会显示：
  - 已处理消息条数
  - 最近桥接错误（如有）
- 微信编辑弹窗也会显示桥接是否已启动、最近运行时间、错误状态

### 当前验证结果

- `get_wechat_bridge_status` 返回：
  - `bridge.started = true`
- 桥接状态文件已写入：
  - `~/.openclaw/clawpanel/wechat-bridge-state.json`
- 已确认至少处理过 1 条微信消息：
  - `handledCount = 1`
  - `lastMessageAt` 已更新

### 仍需继续观察

- 目前桥接主链路已经跑通，但调试入口 `wechat_bridge_test_message` 仍出现过一次 `ws read timeout`
- 后续如继续稳定性优化，优先检查：
  - Gateway 握手阶段是否偶发收到 ping/frame 干扰
  - 长回复/工具调用场景下 `chat.send` 的最终帧等待策略
  - 是否需要把桥接日志单独写入 `~/.openclaw/logs/wechat-bridge.log`

### 2026-03-23 下午补充：微信图片回复能力定位

用户继续测试后发现：

- 微信里发消息，Agent 已经能自动回复文本
- 但请求“截图电脑屏幕”这类需要图片输出的场景，微信侧没有收到图片

排查结论：

- 当前桥接实现只会提取 Gateway 最终回复中的 **文本内容**
- 然后固定调用 `wechat.send_text_message`
- 已安装的 `mcp-wechat-server@1.0.1` 实际只暴露 6 个工具：
  - `login_qrcode`
  - `check_qrcode_status`
  - `logout`
  - `get_messages`
  - `send_text_message`
  - `send_typing`
- **没有 `send_image` / `send_file` / 上传媒体** 类工具

因此这不是前端渲染问题，也不是桥接漏调某个隐藏接口，而是：

- 当前这条微信接入链路本质上是 **文本型 MCP 微信通道**
- 自动回复文本没问题
- 自动回发图片/文件目前做不到

#### 本次补丁

- `scripts/dev-api.js`
  - 新增对 Gateway 最终回复中图片/文件线索的提取
  - 当检测到回复包含媒体内容时，不再静默丢弃
  - 会自动降级为一条明确提示：
    - 当前 `mcp-wechat-server 1.0.1` 只支持文本消息
    - 本次结果包含媒体内容，但已降级为文本说明

这样用户在微信里至少能收到**明确解释**，而不是看起来像“机器人没发出来 / 坏了”。

#### 后续方案

如果要实现真正的“微信里直接收图片/文件”，有两条路：

1. 给 `mcp-wechat-server` 增加媒体发送工具（如 `send_image_message` / `send_file_message`）
2. 改用 OpenClaw 社区的微信原生插件通道（文档里记录的 `@icesword760/openclaw-wechat`），让微信像 Telegram / 飞书一样走原生渠道能力

---

## 腾讯官方原生微信插件回归验证

> 时间: 2026-03-23 下午

### 测试目标

验证腾讯官方原生微信安装器：

```bash
npx -y @tencent-weixin/openclaw-weixin-cli@latest install
```

是否能够替代当前 `mcp-wechat-server` 方案，并确认其是否具备图片发送能力。

### 结果摘要

- 官方安装器包已确认存在，npm 包名：
  - `@tencent-weixin/openclaw-weixin-cli`
- 官方原生插件已实际安装到本机：
  - `~/.openclaw/extensions/openclaw-weixin`
- 插件 `README.zh_CN.md` 明确写明后端 `sendMessage` 支持：
  - 文本
  - 图片
  - 视频
  - 文件
- 插件源码中存在实际媒体发送实现：
  - `src/messaging/send-media.ts`
  - `src/messaging/send.ts`
  - `sendImageMessageWeixin`
  - `sendFileMessageWeixin`

结论：

- **腾讯官方原生微信插件具备图片/文件发送能力**
- 这一点不是只看社区介绍，而是已经通过 README + 源码双重确认

### 安装过程中踩到的问题

#### 1. 本机 `openclaw.json` 旧配置阻塞安装

安装器第一次执行时报错：

- `agents: Unrecognized key: "profiles"`

原因：

- 旧配置仍保留 `agents.profiles.weixin`
- 当前 OpenClaw 版本已经改为 `agents.list[].identity`

修复方式：

- 备份原配置
- 将旧的 `agents.profiles.weixin` 迁移为真正的 agent：
  - `agents.list[].id = "weixin"`
  - `identity.name = "微信"`
  - `identity.emoji = "🛰"`

#### 2. 安装器未完成插件启用

安装器执行后，插件目录已写入，但 `plugins.allow` / `plugins.entries` 没有完全就绪。

因此补做：

- `plugins.entries.openclaw-weixin.enabled = true`
- `plugins.allow += openclaw-weixin`

#### 3. 插件依赖未装齐

Gateway 重启后报错：

- `Cannot find module 'zod'`

原因：

- 安装器将插件源码复制到 `~/.openclaw/extensions/openclaw-weixin`
- 但没有把运行依赖装进插件目录

修复方式：

```bash
cd ~/.openclaw/extensions/openclaw-weixin
npm install --omit=dev
```

补完后，Gateway 已能成功加载：

- `openclaw-weixin: loaded`

### 当前验证状态

- `openclaw channels login --channel openclaw-weixin` 已能正常启动
- 终端二维码已成功显示
- 说明官方原生微信插件在本机已达到“可扫码接入”状态

### 后续下一步

完成扫码登录后，应继续验证：

1. 微信文本收发
2. 微信图片发送
3. 微信文件发送
4. ClawPanel 渠道页是否切换回原生微信插件接入流程

---

## ClawPanel 微信渠道切回腾讯官方原生插件

> 时间: 2026-03-23 下午

### 背景

前面虽然已经把微信通过 `mcp-wechat-server` 跑通了文本自动回复，但后续实测发现：

- 微信自动回复图片/文件做不到
- 根因不是前端，而是 MCP 工具本身没有媒体发送接口

因此最终决定把 ClawPanel 的微信渠道实现，从 MCP 方案切回 **腾讯官方原生微信插件**。

### 本次调整

#### 1. 渠道页改回官方插件元数据

- 微信卡片安装包改为：
  - `@tencent-weixin/openclaw-weixin@latest`
- 插件 ID 改为：
  - `openclaw-weixin`
- 接入说明改成原生插件流程：
  - 安装官方插件
  - 执行 `openclaw channels login --channel openclaw-weixin`
  - 扫码登录

#### 2. ClawPanel 后端把 `wechat` 平台映射到真实原生渠道

- 前端平台 ID 仍保持 `wechat`，避免 UI 大改
- 但实际配置键改为：
  - `channels.openclaw-weixin`
- 实际绑定键改为：
  - `openclaw-weixin`

这样页面里仍然叫“微信”，但底层已经是原生插件通道。

#### 3. 微信运行态检测改为读取原生插件状态

不再读取 `~/.mcp-wechat-server/*`，改为读取：

- 插件目录：
  - `~/.openclaw/extensions/openclaw-weixin`
- 账号状态：
  - `~/.openclaw/openclaw-weixin/accounts.json`
  - `~/.openclaw/openclaw-weixin/accounts/*.json`

显示项同步调整为：

- 插件是否安装
- 是否已写入 `channels.openclaw-weixin`
- 是否已有扫码登录账号
- 当前账号数
- 扫码命令提示

#### 4. 官方插件安装后的补处理自动化

官方插件安装后还需要补几步，ClawPanel 已自动处理：

- 自动加入 `plugins.allow`
- 自动设置 `plugins.entries.openclaw-weixin.enabled = true`
- 若插件目录缺依赖，则自动在插件目录执行：
  - `npm install --omit=dev`

这样避免再次出现：

- 插件已落盘但未启用
- Gateway 重启时报 `Cannot find module 'zod'`

#### 5. 清理旧的 MCP 专用前端提示

聊天页原先有一套专门针对：

- `wechat-mcp`
- `login_qrcode`
- `check_qrcode_status`

的会话快照提示和二维码快捷按钮。

切回官方原生插件后，这些逻辑已经不再适用，因此已禁用，避免继续误导用户。

### 涉及文件

| 文件 | 改动 |
|------|------|
| `src/pages/channels.js` | 微信卡片改回腾讯官方原生插件，绑定键改为 `openclaw-weixin` |
| `scripts/dev-api.js` | 微信平台配置/运行态/插件安装后处理切回官方原生插件 |
| `src/pages/chat.js` | 禁用旧的 `wechat-mcp` 专用提示与二维码快捷动作 |

### 当前状态

- 官方微信插件已在本机加载成功
- ClawPanel 微信渠道逻辑已切回原生插件方案
- 下一步只差继续完成扫码登录与收发回归

### 涉及文件

| 文件 | 改动 |
|------|------|
| `scripts/dev-api.js` | 新增微信自动桥接守护循环、Gateway 聊天复用、桥接状态持久化、调试接口 |
| `src/pages/channels.js` | 微信运行态展示桥接状态、错误、已处理消息数 |

## 微信方案最终收口：移除 mcp-wechat-server，保留腾讯官方原生插件

### 背景

在完成多轮微信接入验证后，最终决定不再继续维护 `mcp-wechat-server` 路线，原因是：

- 官方原生插件接入路径更短，登录流程更直接
- 原生通道与 OpenClaw `channels` 体系一致，后续维护成本更低
- 图片/文件等媒体能力以官方原生插件为主，不再继续围绕 MCP 方案做兼容

### 本次收口内容

#### 1. 删除项目里的 MCP 微信残留代码

- 移除 `install_mcp_plugin` 相关前端调用入口
- 移除 `mcp_plugin_install_stream` 流式安装入口
- 移除聊天页中针对 `wechat-mcp` 的旧提示文案
- 移除 `dev-api` 启动时自动拉起旧微信桥接循环的逻辑
- 本地图片白名单不再包含 `~/.mcp-wechat-server`

#### 2. 微信渠道文案统一为官方原生插件

- 微信卡片说明统一为腾讯官方原生插件方案
- 登录指引统一为：
  - `openclaw channels login --channel openclaw-weixin`

#### 3. 最终保留方案

- 渠道配置键：
  - `channels.openclaw-weixin`
- 运行态目录：
  - `~/.openclaw/openclaw-weixin`
- 插件目录：
  - `~/.openclaw/extensions/openclaw-weixin`

### 结果

- ClawPanel 项目代码中已不再保留 `mcp-wechat-server / wechat-mcp` 的有效接入路径
- 微信后续只保留腾讯官方原生插件方案

### 涉及文件

| 文件 | 改动 |
|------|------|
| `src/pages/channels.js` | 微信文案与接入说明最终统一为官方原生插件 |
| `src/pages/chat.js` | 清理旧 `wechat-mcp` 提示与分支 |
| `src/lib/tauri-api.js` | 删除旧 MCP 微信安装 API |
| `scripts/dev-api.js` | 删除旧 MCP 安装流、旧桥接启动、旧本地目录依赖 |

## 消息渠道页性能优化 + 微信弹窗内直接扫码绑定

### 问题

继续测试时，发现两个体验问题：

1. 进入“消息渠道”页明显卡顿  
2. 微信虽然已经改成官方原生插件，但在渠道页里仍然只显示终端命令，没有直接扫码绑定入口

### 原因定位

#### 1. 页面首屏慢的原因

`src/pages/channels.js` 在进入页面时会：

- 先串行加载已接入平台列表
- 再读取微信运行态
- 再读取 `openclaw.json`
- 最后对所有需要插件的渠道逐个检查安装状态

其中“所有插件安装状态探测”最重，而且原来是首屏阻塞的一部分，所以用户会感觉页面打开很卡。

#### 2. 微信不能在弹窗内直接扫码的原因

之前微信配置弹窗只是展示：

- 当前运行态
- 一条 `openclaw channels login --channel openclaw-weixin` 命令

并没有真正调用官方微信插件的二维码登录流程，所以用户必须回到终端手动执行扫码登录。

### 本次修复

#### 1. 渠道页改为“先渲染，再后台补状态”

- 首屏先并行加载：
  - 已接入列表
  - 微信运行态
  - bindings 配置
- 先把页面渲染出来
- 插件安装状态改为后台异步补齐
- 同时增加短时缓存，避免频繁进出页面时重复探测所有插件

这样“消息渠道”页打开时不再等完整插件探测结束后才出内容。

#### 2. 微信配置弹窗内直接生成二维码并轮询绑定状态

新增前端按钮：

- `生成二维码`
- `检查状态`

新增后端接口：

- `start_wechat_qr_login`
- `wait_wechat_qr_login`

实现方式：

- 后端直接按腾讯官方微信插件源码的二维码登录协议发起请求
- 从 `ilink` 接口获取二维码链接
- 在弹窗内直接显示二维码图片
- 前端自动轮询扫码状态
- 扫码确认后，自动保存微信账号凭证并重载 Gateway

这样微信在“消息渠道 -> 编辑/接入微信”里就可以直接扫码绑定，不需要再切去终端。

### 涉及文件

| 文件 | 改动 |
|------|------|
| `src/pages/channels.js` | 首屏改为快速渲染，插件状态后台补齐；微信弹窗新增二维码绑定 UI |
| `src/lib/tauri-api.js` | 新增微信二维码登录接口封装 |
| `scripts/dev-api.js` | 新增官方微信二维码登录/轮询/账号落盘逻辑 |

### 结果

- “消息渠道”页首屏不再被所有插件探测阻塞
- 微信现在可以在渠道页弹窗里直接扫码绑定
- 绑定成功后会自动刷新运行态并重载 Gateway

---

## 2026-03-23 晚间补充：修复官方微信插件“已登录但不通信”

### 现象

- `gateway.log` 里只有 `openclaw-weixin` 插件加载记录，没有 `getUpdates/sendMessage` 相关通信
- `openclaw channels list` 显示：
  - `openclaw-weixin dc34dc2a3f3f-im-bot: not configured, enabled`
- 但本地实际上已经有扫码生成的账号文件和 token

### 根因

ClawPanel 自己实现的二维码绑定落盘逻辑，账号 ID 规范化规则和腾讯官方插件不一致：

- 我们写入的是：
  - `dc34dc2a3f3f-im.bot.json`
  - `accounts.json` 中的 `dc34dc2a3f3f-im.bot`
- 官方插件实际读取的是：
  - `dc34dc2a3f3f-im-bot.json`
  - `accounts.json` 中的 `dc34dc2a3f3f-im-bot`

因此 Gateway 虽然能发现账号索引，但读取不到对应 token，最终把账号判定为 `not configured`，不会启动真正的微信长轮询通信。

### 修复

- 修正 `scripts/dev-api.js` 中的 `normalizeWechatAccountId()`，移除对 `.` 的保留，改为与官方插件一致的规范化结果
- 新增 `migrateWechatAccountStorage()`：
  - 自动重命名旧的账号文件
  - 自动重写 `accounts.json`
  - 在读取微信运行态前自动执行迁移

### 结果

- 后续通过渠道页扫码绑定时，不会再把账号文件写成错误格式
- 老的错误账号文件也能在运行态检测时被自动迁正
- 修复后，Gateway 才能把微信账号识别为 `configured` 并进入正常通信链路
